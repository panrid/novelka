/**
 * The one place that answers «may this person do this?». Services call it before acting;
 * hidden buttons in the web app are only a convenience.
 */
@ApplicationModule(displayName = "Права")
package space.panrid.novelka.access;

import org.springframework.modulith.ApplicationModule;
