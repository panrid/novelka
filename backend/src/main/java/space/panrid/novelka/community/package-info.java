/**
 * Talking about translations: comments on a translation and its chapters, the site chat,
 * mentions of people (@нік) and teams ($команда), votes, ratings and reports.
 */
@ApplicationModule(displayName = "Спільнота")
package space.panrid.novelka.community;

import org.springframework.modulith.ApplicationModule;
