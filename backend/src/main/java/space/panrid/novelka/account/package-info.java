/**
 * Accounts: registration, sign-in, email links, profile. Other modules learn who the
 * visitor is through {@link space.panrid.novelka.account.CurrentUser}.
 */
@ApplicationModule(displayName = "Акаунти")
package space.panrid.novelka.account;

import org.springframework.modulith.ApplicationModule;
