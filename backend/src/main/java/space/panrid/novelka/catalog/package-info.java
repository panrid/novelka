/**
 * Novels, editions and tags: what exists to read. The home page, catalog and novel
 * pages are read models in the reading module.
 */
@ApplicationModule(displayName = "Каталог")
package space.panrid.novelka.catalog;

import org.springframework.modulith.ApplicationModule;
