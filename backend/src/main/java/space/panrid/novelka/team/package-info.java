/**
 * Teams that own editions. Stage 2 needs only the personal team everyone gets;
 * members, roles and names arrive with the Studio.
 */
@ApplicationModule(displayName = "Команди")
package space.panrid.novelka.team;

import org.springframework.modulith.ApplicationModule;
