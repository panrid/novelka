/**
 * Originals from Syosetu: the link, the novel's data from the site's API, and chapter
 * pages turned into blocks. Japanese text stays in the database; it is never shown.
 */
@ApplicationModule(displayName = "Джерела")
package space.panrid.novelka.source;

import org.springframework.modulith.ApplicationModule;
