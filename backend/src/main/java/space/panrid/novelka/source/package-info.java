/**
 * Originals from the source sites: each site is a provider that recognises its links,
 * describes the novel with its table of contents and turns chapter pages into blocks.
 * The original text stays in the database; it is never shown to readers.
 */
@ApplicationModule(displayName = "Джерела")
package space.panrid.novelka.source;

import org.springframework.modulith.ApplicationModule;
