/**
 * Autotranslation «до глави N»: the quote, the queue in PostgreSQL, the pipeline
 * analyze → translate → proofread → publish with checkpoints, and the glossary.
 * At launch only the site owner uses it (рішення 23).
 */
@ApplicationModule(displayName = "Автопереклад")
package space.panrid.novelka.autotranslate;

import org.springframework.modulith.ApplicationModule;
