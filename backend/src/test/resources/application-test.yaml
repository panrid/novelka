novelka:
  limits:
    sign-ins-per-address: 1000
    sign-ins-per-login: 1000
    registrations-per-address: 1000
  media:
    dir: build/test-media
  # Tests run the queue themselves (Worker.drain) instead of on a schedule.
  autotranslate:
    worker: false
