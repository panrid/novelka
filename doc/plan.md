# План розробки

Статус: пропозиція на погодження. Кожен етап — кілька комітів, один коміт на завершену
задачу. Етап закінчується тим, що його можна показати на телефоні й він покритий тестами.

Порядок вибрано так, щоб якомога раніше отримати сайт, який можна читати, а гроші й
автопереклад будувати на вже готових ревізіях і командах.

## 0. Каркас
- Gradle: `backend` (Spring Boot, Flyway, генерація jOOQ через Testcontainers),
  `frontend` (Vite, React, TS, TanStack Router і Query, React Aria).
- `infra/compose.yaml` з PostgreSQL. Одна команда локального запуску.
- Токени теми (темна за замовчуванням), базова оболонка з нижньою панеллю, сторінка помилки.
- Spring Modulith: перевірка меж модулів у тестах.
- **Результат:** порожня оболонка відкривається на телефоні, `./gradlew check` зелений.

## 1. Акаунти
- Реєстрація, вхід (email або нік), сесії в базі, CSRF, обмеження спроб.
- Підтвердження email і відновлення пароля листами.
- Профіль «про себе», публічна сторінка `/u/{нік}`, налаштування акаунта.
- Роль сайту, створення власника при першому запуску. Каркас `AccessPolicy`.

## 2. Читання
- Новели, переклади, теги, глави й ревізії з явною публікацією.
- Тимчасове наповнення: імпорт готового тексту адміністратором
  (щоб було що читати до етапу 3).
- Головна (продовжити, популярне, нові глави), каталог, сторінка новели, читалка
  з панеллю, що з'являється від дотику.
- Бібліотека й прогрес читання між пристроями.
- **Результат:** можна читати з телефона.

## 3. Команди і Студія
- Команди, учасники, ролі, назва команди, режим редакторів.
- Студія: мої переклади, новий переклад вручну, дані перекладу, глави.
- Повний редактор глави, автозбереження, публікація, історія ревізій і diff.
- Внесок кожного на сторінці перекладу й у профілі.

## 4. Правки
- Пропозиції в читалці (абзац, «замінити всі входження»), пакет, відкликання.
- Режим перевірки в читалці для редакторів: прийняти, відхилити, змінити й прийняти.
- Черга правок у Студії, стан `stale` при конфлікті, «Мої правки».

## 5. Кроки (внутрішня валюта)
- Книга записів у кроках: рахунки користувачів і команд, резерви.
- Ручне нарахування власником сайту, пакети кроків, переказ власником команди в її бюджет.
- Сторінка «Кроки», звіт «собівартість кроку» й фінанси в адмінці.
- Порт `PaymentProvider` з адаптером ручного нарахування.

## 6. Автопереклад
- Імпорт із Syosetu (метадані, список і тексти глав).
- Кошторис «до глави N» у кроках з урахуванням уже перекладеного.
- Автоматичне доведення до кінця: невдалий крок, який гарантовано не оплачено, повторюється сам.
- Естафета: «покинутий» переклад, запит «Хочу продовжити», продовження іншою командою.
- Черга в Postgres, конвеєр аналіз → переклад → вичитка, словник і пропозиції словника.
- Журнал AI-викликів. Кроки резервуються на весь діапазон, списуються за кожну
  опубліковану главу, а за неперекладені глави (скасування, збій) повертаються.
- Прогрес через SSE, «Скасувати», зрозумілі помилки.
- **Результат:** «вставив посилання → вказав главу → побачив ціну → запустив».

## 7. Спільнота і вхідні
- Коментарі до перекладу й глави (обговорення відкривається плаваючою кнопкою), чат.
- Розмітка, згадки людей і команд, відповіді, цитати.
- Голоси за коментарі, оцінка якості перекладу.
- Вхідні: сповіщення через SSE, тег команди — власнику й перекладачам.

## 8. Ілюстрації
- Завантаження картинки в редакторі, блок `image` у ревізії.
- Генерація за виділеним фрагментом з бюджету команди, з показом ціни.

## 9. Адміністрування
- Модерація коментарів, чату й перекладів, журнал дій, налаштування моделей.
- Огляд черги перекладів і витрат провайдера.

## 10. Онлайн-оплата
- Адаптер вибраного платіжного сервісу, webhook, повернення.
- Лише після рішення щодо сервісу й консультації з бухгалтером.

## 11. Запуск
- Деплой (VPS, Caddy, резервні копії бази, моніторинг).
- За рішенням власника — перенесення даних із v1.

## Рішення власника

Погоджено 2026-09-24.

1. **Кілька перекладів однієї новели** від різних команд — так. Якщо перекладів кілька,
   на сторінці новели є перемикач. За замовчуванням показується найпопулярніший.
2. **Команда може мати багато перекладів.** Нова людина отримує особисту команду
   з назвою = нік.
3. **Бюджет команди поповнює лише власник команди** зі своїх кроків. Витрачають
   (автопереклад, ілюстрації) власник і перекладачі.
4. **Естафета перекладу.** Переклад стає вільним для продовження, якщо:
   - власник сам позначив його «покинутим», **або**
   - власник не заходив на сайт X місяців (налаштування сайту, за замовчуванням 3), **або**
   - *(пропозиція, на погодження)* нових глав не було X місяців, хтось натиснув
     «Хочу продовжити», а власник не відповів за 14 днів. Це правило закриває випадок,
     коли власник заходить на сайт, але переклад не веде.

   Як це працює:
   - Інша команда створює **свій** переклад, починаючи з наступної глави.
     Попередні глави лишаються за першою командою, з її авторством.
   - *(пропозиція)* Нова команда може скопіювати словник попереднього перекладу,
     щоб імена й терміни не змінилися посеред історії.
   - Читалка веде далі без розриву: після останньої глави першої команди — «Продовження
     від команди Y» і перехід на главу N+1 нового перекладу.
   - Гроші в кожного свої: нічого не переказується й не повертається.
   - Якщо перша команда повернеться, вона продовжує свій переклад. Існуватимуть два
     переклади, що дозволено пунктом 1.
5. **Внутрішня валюта — «кроки».** Гривні на акаунті не показуються.
   - 1 крок = переклад однієї глави. Орієнтир: 1 крок ≈ $0,05–0,10 при продажу.
     Реальна собівартість зараз ≈ $0,02 на главу (gpt-4o-mini, див. аудит). Запас
     покриває комісії, податки, курс і дорожчі глави.
   - *(пропозиція)* Довга глава коштує більше: 1 крок за кожні повні або неповні
     10 000 знаків оригіналу. Інакше глава на 30 000 знаків коштуватиме як на 3 000.
     Точна межа — після статистики.
   - Поповнення — пакетами кроків (наприклад, «100 кроків за N ₴»). Ціни пакетів
     задає власник сайту.
   - Спершу власник перекладає сам. Після 1000–2000 глав за звітом «собівартість кроку»
     в адмінці визначається ціна пакетів.
   - Облік у книзі записів — у кроках. Гроші платежів (₴) і собівартість моделей ($)
     зберігаються окремо для звітів.
   - Модель для автоперекладу обирає сайт, а не користувач: ціна кроку прив'язана
     до її вартості.
6. **Правки пропонують усі, погоджують лише перекладачі цього перекладу й призначені
   редактори.** Режиму «редактори — усі» більше немає. Повний редактор для звичайного
   користувача зберігає зміни як пропозицію до глави.
7. **Правки надсилаються пакетом.** Прийнятий пакет дає одну ревізію на кожну змінену
   главу, а не по ревізії на правку (у v1 глава 1 мала 54 ревізії). Щоб чернетки не
   губилися:
   - вони зберігаються на сервері;
   - у читалці видно лічильник «3 правки не надіслано»;
   - наприкінці глави є нагадування.

## Відкриті питання
1. Пункти 4 і 5, позначені «пропозиція»: правило «немає глав + запит без відповіді»,
   копіювання словника, ціна довгої глави.
2. Назва валюти: «кроки» — робоча назва.
3. Платіжний сервіс (власник не ФОП) і перенесення даних з v1 — як у брифі.
