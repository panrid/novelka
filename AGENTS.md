# Робота з Новелкою

- Почни з `doc/brief.md`: аудиторія, вимоги, технічний напрям і відкриті питання.
- Проєкт пишеться з нуля. Попередня версія — `~/IdeaProjects/novelka-v1`: її `doc/` можна
  читати як специфікацію поведінки й досвід, але код звідти не копіюємо.
- Код не пишемо, доки власник не погодив структуру й план.
- Стек: Java + Spring Boot + jOOQ + Flyway + PostgreSQL; React + TypeScript (mobile-first).
- Інтерфейс і тексти для користувачів — українською.
- Віддаленого репозиторію поки немає: лише коміти, без push. Один коміт на завершену задачу.
- Не комітити секрети, `.env`-файли, ключі API й SSH-ключі.
- Не заявляти успіх тестів, які не запускались або не пройшли.
